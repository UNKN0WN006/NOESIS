# ThreatLens AI - Frontend (Next.js) starter

This folder contains starter components and pages for the ThreatLens AI dashboard.

Notes:
- The frontend expects the backend API at `http://localhost:8000/analyze` for local testing.
- Use `npm run dev` after installing dependencies.
